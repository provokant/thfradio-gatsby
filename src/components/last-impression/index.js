import React from "react"

import Hero from "../hero"
import Container from "../container"

import "./last-impression.scss"

const LastImpression = () => {
  return (
    <section className="last-impression">
      <Container>
        <Hero/>
      </Container>
    </section>
  )
}

export default LastImpression
